{
  "name": "essay-grader",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev ./public",
    "deploy": "wrangler pages deploy ./public --project-name essay-grader",
    "secret": "wrangler pages secret put QWEN_API_KEY --project-name essay-grader"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
